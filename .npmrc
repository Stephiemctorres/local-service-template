# Prevents Playwright (a transitive dependency of the image-processing plugin)
# from trying to download a Chromium browser during install — that download
# fails on some Mac setups and isn't needed for building or serving this site.
ignore-scripts=true
